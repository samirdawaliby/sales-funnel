{
  "name": "caplogy-funnel",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Caplogy marketing funnel — registration, Discord, email",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute caplogy-db --file=db/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
